<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kamabi">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kamabi">
        <activity android:name=".OpeningQuizActivity"></activity>
        <activity android:name=".KuisActivity" />
        <activity android:name=".PenerapanActivity" />
        <activity
            android:name=".KeyboardActivity"
            android:label="Kitab Asli">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".KitabActivity" />
        <activity android:name=".SplahActivity" />
        <activity android:name=".OnboardingActivity"></activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>